# Leonardo Souza Martins - Exercício 8.1

## `entregas/14-leonardo-souza-martins/1C9B6csM6ALfZAwPmroXtt7-MRcxQvTsT.ipynb` - cell 091 `code`

```text
# Exercício 8.1 — Recalibrando uma distribuição e avaliando o impacto

np.random.seed(42)

pesos_cenario = [0.25, 0.25, 0.20, 0.30]

df_cenario = gerar_base(
    n_sessoes=5000,
    pesos_perfil_override=pesos_cenario
)

taxa_original = df["clicou_comunicado"].mean()
taxa_cenario = df_cenario["clicou_comunicado"].mean()
variacao_pp = (taxa_cenario - taxa_original) * 100

print("Comparação da taxa de clique em comunicados")
print()
print(f"Base original: {taxa_original:.1%}")
print(f"Cenário com Operacional = 30%: {taxa_cenario:.1%}")
print(f"Variação observada: {variacao_pp:+.1f} pp")
print()

# Comparação da composição dos perfis
comparacao_perfis = pd.DataFrame({
    "perfil_usuario": perfis,
    "peso_referencia_original": pesos_perfil,
    "peso_cenario_planejado": pesos_cenario,
})

freq_original = (
    df["perfil_usuario"]
    .value_counts(normalize=True)
    .rename("freq_original_observada")
    .reset_index()
    .rename(columns={"index": "perfil_usuario"})
)

freq_cenario = (
    df_cenario["perfil_usuario"]
    .value_counts(normalize=True)
    .rename("freq_cenario_observada")
    .reset_index()
    .rename(columns={"index": "perfil_usuario"})
)

comparacao_perfis = (
    comparacao_perfis
    .merge(freq_original, on="perfil_usuario")
    .merge(freq_cenario, on="perfil_usuario")
)

comparacao_perfis["desvio_cenario_vs_referencia_pp"] = (
    (comparacao_perfis["freq_cenario_observada"] - comparacao_perfis["peso_referencia_original"]) * 100
).round(1)

display(
    comparacao_perfis.assign(
        peso_referencia_original=lambda d: d["peso_referencia_original"].map(lambda x: f"{x:.1%}"),
        peso_cenario_planejado=lambda d: d["peso_cenario_planejado"].map(lambda x: f"{x:.1%}"),
        freq_original_observada=lambda d: d["freq_original_observada"].map(lambda x: f"{x:.1%}"),
        freq_cenario_observada=lambda d: d["freq_cenario_observada"].map(lambda x: f"{x:.1%}"),
        desvio_cenario_vs_referencia_pp=lambda d: d["desvio_cenario_vs_referencia_pp"].map(lambda x: f"{x:+.1f} pp"),
    )
)

maior_desvio = comparacao_perfis["desvio_cenario_vs_referencia_pp"].abs().max()

print()
print("Avaliação do cenário")
print()

if taxa_cenario > taxa_original:
    print(
    "A taxa de clique ficou praticamente estável, com aumento marginal. "
    "Mesmo assim, o cenário altera fortemente a composição dos perfis e compromete o eixo de Cobertura."
  )
else:
    print("A taxa de clique não aumentou de forma relevante no cenário observado.")

print(
    "A mudança é defensável se for tratada como simulação hipotética: "
    "'e se o perfil Operacional representasse 30% da base?'."
)

if maior_desvio > 2:
    print(
        "Porém, ela distorce o eixo de Cobertura do scorecard, "
        "porque a distribuição de perfis deixa de representar os pesos originais do IPT."
    )
else:
    print(
        "A mudança não gerou desvio relevante de cobertura em relação à referência original."
    )

print()
print("Veredicto:")
print(
    "DEFENSÁVEL COMO CENÁRIO HIPOTÉTICO; "
    "NÃO DEFENSÁVEL COMO SUBSTITUTA DA BASE ORIGINAL SEM DOCUMENTAR A RECALIBRAÇÃO."
)

[outputs]
Comparação da taxa de clique em comunicados

Base original: 29.2%
Cenário com Operacional = 30%: 29.3%
Variação observada: +0.1 pp


   perfil_usuario peso_referencia_original peso_cenario_planejado freq_original_observada freq_cenario_observada  \
0     Pesquisador                    30.0%                  25.0%                   30.4%                  26.1%   
1  Administrativo                    27.0%                  25.0%                   27.3%                  24.9%   
2         Tecnico                    25.0%                  20.0%                   24.4%                  19.5%   
3     Operacional                    18.0%                  30.0%                   17.9%                  29.5%   

  desvio_cenario_vs_referencia_pp  
0                         -3.9 pp  
1                         -2.1 pp  
2                         -5.5 pp  
3                        +11.5 pp  

Avaliação do cenário

A taxa de clique aumentou no cenário com maior participação do perfil Operacional.
A mudança é defensável se for tratada como simulação hipotética: 'e se o perfil Operacional representasse 30% da base?'.
Porém, ela distorce o eixo de Cobertura do scorecard, porque a distribuição de perfis deixa de representar os pesos originais do IPT.

Veredicto:
DEFENSÁVEL COMO CENÁRIO HIPOTÉTICO; NÃO DEFENSÁVEL COMO SUBSTITUTA DA BASE ORIGINAL SEM DOCUMENTAR A RECALIBRAÇÃO.
```
