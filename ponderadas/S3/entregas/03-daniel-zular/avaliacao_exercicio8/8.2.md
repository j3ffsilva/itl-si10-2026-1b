# Daniel Zular - Exercício 8.2

## `entregas/03-daniel-zular/1S2Q8nHNL6BCZFAdqErCE6aijGVbehmn5.ipynb` - cell 093 `code`

```text
# Implementação da versão modificada de gerar_tempos e comparação com a base original.

def gerar_tempos_uniforme(objetivo, usou_busca, faixa_horaria):
    """Versão modificada: tempo de leitura com distribuição uniforme em vez de log-normal."""
    params_busca = {
        'buscar_sistema': (4.0, 0.55), 'buscar_documento': (4.0, 0.55),
        'buscar_pessoa': (4.0, 0.55), 'ler_comunicado': (2.9, 0.45),
        'acompanhar_noticias': (3.2, 0.50),
    }
    mu_b, sig_b = params_busca.get(objetivo, (3.5, 0.50))
    tempo_busca = np.random.lognormal(mu_b, sig_b)

    lim_inf = 30
    lim_sup = 400 if objetivo in {'ler_comunicado', 'acompanhar_noticias'} else 200
    tempo_leitura = np.random.uniform(lim_inf, lim_sup)

    if not usou_busca:
        tempo_busca *= 0.45
    if faixa_horaria == '08-11':
        tempo_leitura *= 0.88
        tempo_busca *= 1.08

    ruido = np.random.gamma(shape=2.0, scale=8.0)
    tempo_total = tempo_busca + tempo_leitura + ruido
    return tempo_busca, tempo_leitura, tempo_total


def ajustar_lognormal_e_ks(amostra):
    shape, loc, scale = stats.lognorm.fit(amostra, floc=0)
    ks, p = stats.kstest(amostra, 'lognorm', args=(shape, loc, scale))
    return pd.Series({
        'shape': shape,
        'scale': scale,
        'ks': ks,
        'p_valor': p,
    })


np.random.seed(42)
df_uniforme = gerar_base(n_sessoes=5000, gerar_tempos_fn=gerar_tempos_uniforme)

ks_original = ajustar_lognormal_e_ks(df['tempo_leitura_segundos'])
ks_uniforme = ajustar_lognormal_e_ks(df_uniforme['tempo_leitura_segundos'])

coerencia = pd.DataFrame({
    'base_original': [
        df.loc[df['clicou_comunicado'] == 0, 'tempo_leitura_segundos'].median(),
        df.loc[df['clicou_comunicado'] == 1, 'tempo_leitura_segundos'].median(),
        df.loc[df['clicou_comunicado'] == 1, 'tempo_leitura_segundos'].median() - df.loc[df['clicou_comunicado'] == 0, 'tempo_leitura_segundos'].median(),
        stats.skew(df['tempo_leitura_segundos']),
    ],
    'base_uniforme': [
        df_uniforme.loc[df_uniforme['clicou_comunicado'] == 0, 'tempo_leitura_segundos'].median(),
        df_uniforme.loc[df_uniforme['clicou_comunicado'] == 1, 'tempo_leitura_segundos'].median(),
        df_uniforme.loc[df_uniforme['clicou_comunicado'] == 1, 'tempo_leitura_segundos'].median() - df_uniforme.loc[df_uniforme['clicou_comunicado'] == 0, 'tempo_leitura_segundos'].median(),
        stats.skew(df_uniforme['tempo_leitura_segundos']),
    ]
}, index=[
    'mediana_nao_clicou',
    'mediana_clicou',
    'diferenca_medianas',
    'assimetria_skew',
])

aderencia = pd.DataFrame({
    'base_original': ks_original,
    'base_uniforme': ks_uniforme,
})

print('EXERCÍCIO 8.2 — TROCA DE LOG-NORMAL POR UNIFORME')
print('-' * 95)
print('Aderência de tempo_leitura_segundos a uma log-normal ajustada')
display(aderencia.round(4))

print('Coerência entre tempo_leitura_segundos e clicou_comunicado')
display(coerencia.round(2))

print('Interpretação:')
print(f"- Na base original, o teste KS resulta em p = {ks_original['p_valor']:.4f}.")
print(f"- Na base uniforme, o teste KS resulta em p = {ks_uniforme['p_valor']:.4e}.")
print('- O p-valor cai de forma acentuada na base uniforme, indicando pior aderência ao formato log-normal.')
print('- Ao mesmo tempo, a diferença entre medianas de quem clicou e de quem não clicou continua positiva.')
print('- Em outras palavras, a coerência interna é mantida, mas a fidelidade estatística diminui.')

[outputs]
EXERCÍCIO 8.2 — TROCA DE LOG-NORMAL POR UNIFORME
-----------------------------------------------------------------------------------------------
Aderência de tempo_leitura_segundos a uma log-normal ajustada

         base_original  base_uniforme
shape           0.7614         0.6221
scale          34.0239       120.4557
ks              0.0206         0.0459
p_valor         0.0287         0.0000
Coerência entre tempo_leitura_segundos e clicou_comunicado

                    base_original  base_uniforme
mediana_nao_clicou          25.66         114.44
mediana_clicou              61.18         202.68
diferenca_medianas          35.53          88.23
assimetria_skew              2.62           0.97
Interpretação:
- Na base original, o teste KS resulta em p = 0.0287.
- Na base uniforme, o teste KS resulta em p = 1.3133e-09.
- O p-valor cai de forma acentuada na base uniforme, indicando pior aderência ao formato log-normal.
- Ao mesmo tempo, a diferença entre medianas de quem clicou e de quem não clicou continua positiva.
- Em outras palavras, a coerência interna é mantida, mas a fidelidade estatística diminui.
```
