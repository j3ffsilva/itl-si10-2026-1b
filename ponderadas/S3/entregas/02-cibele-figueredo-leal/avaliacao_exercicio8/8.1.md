# Cibele Figueredo Leal - Exercício 8.1

## `entregas/02-cibele-figueredo-leal/1aJvFvR6e_KjuFTEifULfiVvRx4ftlKA4.ipynb` - cell 094 `code`

```text
# Exercício 8.1 — Recalibrando uma distribuição e avaliando o impacto

np.random.seed(42)

df_cenario_operacional = gerar_base(
    n_sessoes=5000,
    pesos_perfil_override=[0.25, 0.25, 0.20, 0.30]
)

taxa_original = df['clicou_comunicado'].mean()
taxa_cenario = df_cenario_operacional['clicou_comunicado'].mean()

variacao_pp = (taxa_cenario - taxa_original) * 100

comparativo_clique = pd.DataFrame({
    'base': [
        'Base original',
        'Cenário com Operacional = 30%'
    ],
    'taxa_clicou_comunicado': [
        taxa_original,
        taxa_cenario
    ]
})

print('Exercício 8.1 — Recalibrando uma distribuição')
print('-' * 70)
display(comparativo_clique)

print()
print(f'Taxa original de clique em comunicado: {taxa_original:.2%}')
print(f'Taxa no novo cenário: {taxa_cenario:.2%}')
print(f'Variação observada: {variacao_pp:+.2f} pontos percentuais')
print()

if taxa_cenario > taxa_original:
    print(
        'A taxa de clique aumentou no cenário recalibrado. Isso é coerente com a hipótese, '
        'pois o perfil Operacional tem maior ligação com comunicados internos.'
    )
else:
    print(
        'A taxa de clique não aumentou como esperado. Nesse caso, seria necessário revisar '
        'se o perfil Operacional realmente concentra maior interesse em comunicados na base gerada.'
    )

print()
print(
    'Avaliação: essa mudança é defensável como simulação de cenário. Ela ajuda a responder '
    'o que aconteceria se a composição da base tivesse mais usuários do perfil Operacional.'
)

print()
print(
    'Porém, ela distorce o eixo de cobertura se for apresentada como base real, porque os pesos '
    'dos perfis deixam de representar os sinais originais usados para construir a base. Portanto, '
    'o cenário deve ser documentado como hipótese e não como retrato fiel da população observada.'
)

[outputs]
Exercício 8.1 — Recalibrando uma distribuição
----------------------------------------------------------------------

                            base  taxa_clicou_comunicado
0                  Base original                  0.2924
1  Cenário com Operacional = 30%                  0.2932

Taxa original de clique em comunicado: 29.24%
Taxa no novo cenário: 29.32%
Variação observada: +0.08 pontos percentuais

A taxa de clique aumentou no cenário recalibrado. Isso é coerente com a hipótese, pois o perfil Operacional tem maior ligação com comunicados internos.

Avaliação: essa mudança é defensável como simulação de cenário. Ela ajuda a responder o que aconteceria se a composição da base tivesse mais usuários do perfil Operacional.

Porém, ela distorce o eixo de cobertura se for apresentada como base real, porque os pesos dos perfis deixam de representar os sinais originais usados para construir a base. Portanto, o cenário deve ser documentado como hipótese e não como retrato fiel da população observada.
```
