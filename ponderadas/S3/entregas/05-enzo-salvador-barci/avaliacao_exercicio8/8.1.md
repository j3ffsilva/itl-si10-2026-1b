# Enzo Salvador Barci - Exercício 8.1

## `entregas/05-enzo-salvador-barci/1cXJBmX0VLlshjWak_1cjx5o_IRKA6W2q.ipynb` - cell 089 `code`

```text
np.random.seed(42)
df_cenario = gerar_base(n_sessoes=5000, pesos_perfil_override=[0.25, 0.25, 0.20, 0.30])

taxa_original = df['clicou_comunicado'].mean()
taxa_cenario  = df_cenario['clicou_comunicado'].mean()

print(f'Taxa de clique em comunicados:')
print(f'  Base original (Operacional=18%): {taxa_original:.1%}')
print(f'  Cenário (Operacional=30%):       {taxa_cenario:.1%}')
print(f'  Variação: {(taxa_cenario - taxa_original)*100:+.1f} pp')
print()
print('Avalie: essa diferença é relevante para a decisão de negócio?')
print('Avalie: algum eixo do scorecard fica comprometido neste cenário?')

[outputs]
Taxa de clique em comunicados:
  Base original (Operacional=18%): 29.2%
  Cenário (Operacional=30%):       29.3%
  Variação: +0.1 pp

Avalie: essa diferença é relevante para a decisão de negócio?
Avalie: algum eixo do scorecard fica comprometido neste cenário?
```

## `entregas/05-enzo-salvador-barci/1cXJBmX0VLlshjWak_1cjx5o_IRKA6W2q.ipynb` - cell 090 `markdown`

```text
Resposta:

Usando o cenário com pesos_perfil_override=[0.25, 0.25, 0.20, 0.30], a base passa a representar o perfil Operacional como 30% da amostra, em vez dos 18% da base original. O código do notebook gerou a seguinte comparação:
```

## `entregas/05-enzo-salvador-barci/1cXJBmX0VLlshjWak_1cjx5o_IRKA6W2q.ipynb` - cell 091 `code`

```text
np.random.seed(42)
df_cenario = gerar_base(
    n_sessoes=5000,
    pesos_perfil_override=[0.25, 0.25, 0.20, 0.30]
)

taxa_original = df['clicou_comunicado'].mean()
taxa_cenario  = df_cenario['clicou_comunicado'].mean()

print(f'Taxa de clique em comunicados:')
print(f'  Base original (Operacional=18%): {taxa_original:.1%}')
print(f'  Cenário (Operacional=30%):       {taxa_cenario:.1%}')
print(f'  Variação: {(taxa_cenario - taxa_original)*100:+.1f} pp')
```

## `entregas/05-enzo-salvador-barci/1cXJBmX0VLlshjWak_1cjx5o_IRKA6W2q.ipynb` - cell 092 `markdown`

```text
O resultado foi:

Base original: 29,2%
Cenário com Operacional = 30%: 29,3%
Variação: +0,1 ponto percentual

A mudança praticamente não altera a taxa geral de clicou_comunicado. Mesmo que o perfil Operacional tenha maior interesse relativo em comunicados, o impacto agregado foi muito pequeno neste sorteio da base. Do ponto de vista de decisão de negócio, essa diferença de +0,1 p.p. não parece relevante para sustentar uma conclusão de aumento real de engajamento.

A mudança pode ser defensável apenas se for apresentada como um cenário hipotético de simulação, ou seja, uma análise do tipo “e se a composição da base fosse diferente?”. Porém, ela distorce o eixo de cobertura do scorecard se for tratada como uma base representativa da realidade do IPT, porque os pesos de perfil deixam de refletir os parâmetros originais observados/modelados. Portanto, a base recalibrada não melhora necessariamente a qualidade da análise; ela serve mais como teste de sensibilidade do que como substituição da base original.
```
