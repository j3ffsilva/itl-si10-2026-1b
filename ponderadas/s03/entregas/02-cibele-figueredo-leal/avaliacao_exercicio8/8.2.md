# Cibele Figueredo Leal - Exercício 8.2

## `entregas/02-cibele-figueredo-leal/1aJvFvR6e_KjuFTEifULfiVvRx4ftlKA4.ipynb` - cell 097 `code`

```text
# Implemente sua versão modificada de gerar_tempos abaixo e gere uma nova base

def gerar_tempos_uniforme(objetivo, usou_busca, faixa_horaria):
    """Versão modificada: tempo de leitura com distribuição uniforme em vez de log-normal."""
    params_busca = {
        'buscar_sistema': (4.0, 0.55), 'buscar_documento': (4.0, 0.55),
        'buscar_pessoa': (4.0, 0.55), 'ler_comunicado': (2.9, 0.45),
        'acompanhar_noticias': (3.2, 0.50),
    }
    mu_b, sig_b = params_busca.get(objetivo, (3.5, 0.50))
    tempo_busca = np.random.lognormal(mu_b, sig_b)  # mantemos log-normal na busca

    # *** MODIFICAÇÃO: tempo de leitura agora é uniforme ***
    # Ajuste os limites para manter uma média similar
    lim_inf = 30
    lim_sup = 400 if objetivo in {'ler_comunicado', 'acompanhar_noticias'} else 200
    tempo_leitura = np.random.uniform(lim_inf, lim_sup)

    if not usou_busca:
        tempo_busca *= 0.45
    if faixa_horaria == '08-11':
        tempo_leitura *= 0.88
        tempo_busca   *= 1.08
    ruido = np.random.gamma(shape=2.0, scale=8.0)
    tempo_total = tempo_busca + tempo_leitura + ruido
    return tempo_busca, tempo_leitura, tempo_total


np.random.seed(42)
df_uniforme = gerar_base(n_sessoes=5000, gerar_tempos_fn=gerar_tempos_uniforme)

# Compare coerência interna: diferença de tempo de leitura por grupo
mediana_original_0 = df[df['clicou_comunicado'] == 0]['tempo_leitura_segundos'].median()
mediana_original_1 = df[df['clicou_comunicado'] == 1]['tempo_leitura_segundos'].median()
mediana_unif_0 = df_uniforme[df_uniforme['clicou_comunicado'] == 0]['tempo_leitura_segundos'].median()
mediana_unif_1 = df_uniforme[df_uniforme['clicou_comunicado'] == 1]['tempo_leitura_segundos'].median()

print('Comparação de coerência interna: mediana de tempo_leitura por grupo de clique em comunicado')
print(f'{'':30} {"Não clicou":>12} {"Clicou":>12} {"Diferença":>12}')
print(f'  Base original (log-normal):  {mediana_original_0:>12.0f}s {mediana_original_1:>12.0f}s {mediana_original_1 - mediana_original_0:>+12.0f}s')
print(f'  Base modificada (uniforme):  {mediana_unif_0:>12.0f}s {mediana_unif_1:>12.0f}s {mediana_unif_1 - mediana_unif_0:>+12.0f}s')
print()
print('Reflita: qual base preserva melhor a hipótese comportamental do E02?')

[outputs]
Comparação de coerência interna: mediana de tempo_leitura por grupo de clique em comunicado
                                 Não clicou       Clicou    Diferença
  Base original (log-normal):            26s           61s          +36s
  Base modificada (uniforme):           114s          203s          +88s

Reflita: qual base preserva melhor a hipótese comportamental do E02?
```

## `entregas/02-cibele-figueredo-leal/1aJvFvR6e_KjuFTEifULfiVvRx4ftlKA4.ipynb` - cell 098 `code`

```text
# Exercício 8.2 — Enganar o modelo

from scipy.stats import ks_2samp

def gerar_tempos_uniforme_v2(objetivo, usou_busca, faixa_horaria):
    """
    Versão alternativa da função de tempos:
    mantém a busca em formato log-normal, mas troca o tempo de leitura
    para uma distribuição uniforme.
    """

    parametros_busca = {
        'buscar_sistema': (4.0, 0.55),
        'buscar_documento': (4.0, 0.55),
        'buscar_pessoa': (4.0, 0.55),
        'ler_comunicado': (2.9, 0.45),
        'acompanhar_noticias': (3.2, 0.50),
    }

    mu_busca, sigma_busca = parametros_busca.get(objetivo, (3.5, 0.50))
    tempo_busca = np.random.lognormal(mu_busca, sigma_busca)

    if objetivo in ['ler_comunicado', 'acompanhar_noticias']:
        tempo_leitura = np.random.uniform(60, 420)
    elif objetivo == 'buscar_documento':
        tempo_leitura = np.random.uniform(45, 300)
    else:
        tempo_leitura = np.random.uniform(20, 220)

    if not usou_busca:
        tempo_busca *= 0.45

    if faixa_horaria == '08-11':
        tempo_leitura *= 0.88
        tempo_busca *= 1.08

    ruido = np.random.gamma(shape=2.0, scale=8.0)
    tempo_total = tempo_busca + tempo_leitura + ruido

    return tempo_busca, tempo_leitura, tempo_total


np.random.seed(42)

df_uniforme = gerar_base(
    n_sessoes=5000,
    gerar_tempos_fn=gerar_tempos_uniforme_v2
)

# 1. Comparação da distribuição de tempo_leitura entre a base original e a modificada
ks_stat, p_valor_ks = ks_2samp(
    df['tempo_leitura_segundos'],
    df_uniforme['tempo_leitura_segundos']
)

# 2. Coerência entre tempo de leitura e clique em comunicado
coerencia_original = (
    df.groupby('clicou_comunicado')['tempo_leitura_segundos']
    .median()
)

coerencia_uniforme = (
    df_uniforme.groupby('clicou_comunicado')['tempo_leitura_segundos']
    .median()
)

dif_original = coerencia_original.loc[1] - coerencia_original.loc[0]
dif_uniforme = coerencia_uniforme.loc[1] - coerencia_uniforme.loc[0]

comparacao_coerencia = pd.DataFrame({
    'base': ['Original', 'Tempo de leitura uniforme'],
    'mediana_nao_clicou': [
        coerencia_original.loc[0],
        coerencia_uniforme.loc[0]
    ],
    'mediana_clicou': [
        coerencia_original.loc[1],
        coerencia_uniforme.loc[1]
    ],
    'diferenca_clicou_menos_nao_clicou': [
        dif_original,
        dif_uniforme
    ]
})

print('Exercício 8.2 — Enganar o modelo')
print('-' * 70)

print('1. Teste de aderência / comparação de distribuição')
print(f'Estatística KS: {ks_stat:.4f}')
print(f'P-valor: {p_valor_ks:.4f}')
print()

if p_valor_ks < 0.05:
    print(
        'O teste indica que a distribuição de tempo_leitura_segundos mudou de forma relevante '
        'em relação à base original. Isso era esperado, pois a nova função substituiu uma lógica '
        'mais assimétrica por uma distribuição uniforme.'
    )
else:
    print(
        'O teste não detectou diferença estatística forte entre as distribuições, mas ainda assim '
        'é importante verificar visualmente se a forma da distribuição foi artificialmente suavizada.'
    )

print()
print('2. Coerência entre tempo de leitura e clique em comunicado')
display(comparacao_coerencia)

print()
if dif_uniforme > 0:
    print(
        'Na base modificada, quem clicou em comunicado ainda apresenta maior mediana de tempo '
        'de leitura do que quem não clicou. Assim, parte da coerência comportamental foi preservada.'
    )
else:
    print(
        'Na base modificada, a relação esperada entre clique em comunicado e maior tempo de leitura '
        'foi enfraquecida ou invertida. Isso prejudica a coerência interna da base.'
    )

print()
print('3. A nova base é melhor ou pior?')
print(
    'A nova base não é simplesmente melhor ou pior em tudo. Ela pode manter alguma coerência entre '
    'clique e tempo de leitura, mas piora em fidelidade estatística e realismo, porque a distribuição '
    'uniforme elimina parte da cauda e da assimetria esperadas em tempos de navegação. Portanto, ela '
    'é pior no critério de fidelidade/realismo, mesmo que algumas relações internas continuem aceitáveis.'
)

fig = px.histogram(
    pd.concat([
        df[['tempo_leitura_segundos']].assign(base='Original'),
        df_uniforme[['tempo_leitura_segundos']].assign(base='Tempo uniforme')
    ]),
    x='tempo_leitura_segundos',
    color='base',
    barmode='overlay',
    nbins=60,
    opacity=0.65,
    title='Exercício 8.2 | Comparação do tempo de leitura: original vs uniforme'
)

fig

[outputs]
Exercício 8.2 — Enganar o modelo
----------------------------------------------------------------------
1. Teste de aderência / comparação de distribuição
Estatística KS: 0.6896
P-valor: 0.0000

O teste indica que a distribuição de tempo_leitura_segundos mudou de forma relevante em relação à base original. Isso era esperado, pois a nova função substituiu uma lógica mais assimétrica por uma distribuição uniforme.

2. Coerência entre tempo de leitura e clique em comunicado

                        base  mediana_nao_clicou  mediana_clicou  diferenca_clicou_menos_nao_clicou
0                   Original               25.66          61.185                             35.525
1  Tempo de leitura uniforme              138.10         226.250                             88.150

Na base modificada, quem clicou em comunicado ainda apresenta maior mediana de tempo de leitura do que quem não clicou. Assim, parte da coerência comportamental foi preservada.

3. A nova base é melhor ou pior?
A nova base não é simplesmente melhor ou pior em tudo. Ela pode manter alguma coerência entre clique e tempo de leitura, mas piora em fidelidade estatística e realismo, porque a distribuição uniforme elimina parte da cauda e da assimetria esperadas em tempos de navegação. Portanto, ela é pior no critério de fidelidade/realismo, mesmo que algumas relações internas continuem aceitáveis.
```
